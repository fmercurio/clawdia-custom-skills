# Second Brain profile

Owner: {{OWNER}}

Operate the configured vault with provenance, sensitivity, intent-first retrieval, Brain Delta consolidation, and PARA fallback. Missing optional capabilities must degrade gracefully. Never expose restricted content or create cron jobs without explicit authorization.
